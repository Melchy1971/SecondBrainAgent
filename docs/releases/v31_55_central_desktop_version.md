# v31.55 Central Desktop Version

- Fenster, HUD und nativer Status verwenden `secondbrain.version.get_version()`.
- Entfernt die veraltete fest codierte Desktop-Version `30.25`.
- Der Statusreport erhält einen aus der zentralen Version abgeleiteten Dateinamen.
- Öffentliche `VERSION`-Konstanten bleiben als abgeleitete Kompatibilitätsschnittstelle erhalten.
