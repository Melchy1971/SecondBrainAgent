# v31.67 Approval Urgency

The native approval alert now reflects urgency visually. Overdue approvals are
shown with the critical alert color, pending approvals use the warning color,
and an empty queue uses the healthy color. An unavailable snapshot remains a
visible error state.

The severity is derived exclusively from the payload-free approval activity
projection and does not expand access to approval data.
