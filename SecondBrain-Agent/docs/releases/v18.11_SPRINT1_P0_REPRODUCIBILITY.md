# v18.11 Sprint 1 — P0 Reproducibility

## Ziel

Frische Installation und Hygiene-Gates werden reproduzierbar.

## Änderungen

### Packaging

- `pyproject.toml` eingeführt.
- Entry Point `secondbrain = launcher:main` definiert.
- Package Discovery für `secondbrain.*` aktiviert.
- Optionale Extras eingeführt:
  - `dev`
  - `pdf`
  - `connectors`
  - `openai`
  - `all`

### Requirements

- `requirements.txt` als Legacy-Minimalpfad dokumentiert.
- `requirements-dev.txt` auf Dev/Test-Funktion reduziert.
- `requirements-runtime.txt` als kuratierte Runtime-Policy aktualisiert.

### RepoDoctor

RepoDoctor blockiert jetzt zusätzlich:

- fehlendes `pyproject.toml`
- fehlende Packaging-Mindestfelder
- fehlende Requirements-Policy
- README-Verweise auf gelöschte `CHANGELOG_*.md`
- Root-Dateien mit Prefix:
  - `PATCH_`
  - `CHANGELOG_`
  - `VALIDATION_`
- Runtime-/Cache-Artefakte:
  - `runtime/`
  - `logs/`
  - `__pycache__`
  - `.pytest_cache`
  - `*.pid`
  - `*.log`
  - `*.pyc`

### CI

Neuer GitHub Actions Workflow:

```text
.github/workflows/p0-reproducibility.yml
```

Prüft Python 3.11 und 3.12:

```powershell
pip install -e ".[dev]"
python launcher.py repo-doctor --execute-runtime-checks
python launcher.py dependency-inventory
pytest -q tests/test_repo_doctor_v18_7.py
pytest -q tests/test_dependency_inventory_v18_8.py
```

### README / Release Workflow

- Installation auf editable install umgestellt.
- Source of Truth korrigiert.
- Paketverlauf verweist nicht mehr auf gelöschte Root-Changelog-Dateien.
- `docs/RELEASE_WORKFLOW_v18_9.md` auf v18.11-Regeln aktualisiert.

## Validierung lokal

```powershell
cd H:\SecondBrainAgent\SecondBrain-Agent
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -e ".[dev]"
python launcher.py repo-doctor --execute-runtime-checks
python launcher.py dependency-inventory
pytest -q tests/test_repo_doctor_v18_7.py
pytest -q tests/test_dependency_inventory_v18_8.py
```

## Offene Risiken nach Sprint 1

- Vollständiger Gesamttestlauf kann weiterhin Altlasten außerhalb der Hygiene-Gates zeigen.
- Runtime-Core bleibt bewusst standard-library-only.
- Echte optionale Provider/Parser benötigen Extras und weitere Integrationsarbeit.
- PostgreSQL/pgvector, echte Connectoren und produktive Secret-Verschlüsselung sind nicht Teil dieses Sprints.
