# Validation v12.4

Command:

```bash
python -m pytest -q
```

Result:

```text
95 passed
```

Note: Umgebung gab eine unabhängige artifact_tool Spreadsheet-Warmup-Warnung auf stderr aus. Tests selbst sind vollständig grün.
