# Curated runtime dependencies.
#
# The core runtime uses ijson for bounded-memory imports and tzdata for
# deterministic IANA timezone handling on Windows.
ijson>=3.2.3
tzdata>=2025.2
# Optional feature dependencies are declared in pyproject.toml extras:
#
#   pip install -e ".[pdf]"        # PDF parsing via PyMuPDF/pypdf
#   pip install -e ".[connectors]" # connector helpers
#   pip install -e ".[openai]"     # OpenAI SDK integration boundary
#   pip install -e ".[all]"        # all optional features
#
# Do not add test-only packages here.
