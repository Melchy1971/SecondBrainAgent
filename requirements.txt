# Core security and streaming import dependencies.
cryptography>=46.0.3
ijson>=3.2.3

# Remaining feature dependencies are optional.
# Install optional feature sets through pyproject extras, for example:
#   pip install -e ".[dev]"
#   pip install -e ".[pdf]"
#   pip install -e ".[all]"
